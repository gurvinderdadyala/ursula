---
dependencies:
  - role: monitoring-common
  - role: logging-config
    service: neutron
    logdata: "{{ neutron.logs }}"
  - role: openstack-package
    project_name: neutron
    package_alternatives: neutron.console_scripts
    when: openstack_source == 'packages'
